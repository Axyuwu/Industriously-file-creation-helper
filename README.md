# Industriously file creation helper
 A simple script that creates model and texture files automatically for the industriously mod

# Dependencies
 run `pip install -r requirement.txt` to install dependencies

# Run
 run `python src`